import{describe,expect,it}from'vitest';import{input,leak,REST}from'./model';describe('integrate and fire model',()=>{it('sums subthreshold input',()=>expect(input({v:-70,spiked:false},8).v).toBe(-62));it('spikes and resets at threshold',()=>expect(input({v:-54,spiked:false},5).spiked).toBe(true));it('leaks toward rest',()=>{const v=leak({v:-55,spiked:false},1).v;expect(v).toBeLessThan(-55);expect(v).toBeGreaterThanOrEqual(REST)})})
