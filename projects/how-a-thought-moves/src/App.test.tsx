import{fireEvent,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it,vi}from'vitest';import App from'./App';describe('experience flow',()=>{afterEach(()=>vi.useRealTimers());it('moves from landing to guided story and lab',()=>{vi.useFakeTimers();render(<App/>);fireEvent.click(screen.getByRole('button',{name:/follow the signal/i}));expect(screen.getByText('A neuron listens.')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/skip to lab/i}));expect(screen.getByText('INTEGRATE ONE MOMENT')).toBeInTheDocument()})})
