import{fireEvent,render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import App from'./App';describe('experience flow',()=>{it('moves from landing to guided story and lab',()=>{render(<App/>);fireEvent.click(screen.getByRole('button',{name:/make the first prediction/i}));expect(screen.getByText('Predict the next event.')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/skip to lab/i}));expect(screen.getByText('RUN THE UNKNOWABLE')).toBeInTheDocument()})})
