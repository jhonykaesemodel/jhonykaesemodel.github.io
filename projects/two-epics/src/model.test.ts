import{describe,expect,it}from'vitest';import{forEpic}from'./model';describe('epic structure',()=>{it('keeps Iliad told order',()=>expect(forEpic('iliad','told').at(-1)?.id).toBe('priam'));it('reveals the Odyssey flashback',()=>{expect(forEpic('odyssey','told')[0].id).toBe('ithaca');expect(forEpic('odyssey','happened')[0].id).toBe('cyclops')});it('contains 24-book endpoints',()=>expect(forEpic('iliad','told').at(-1)?.book).toBe('24'))})
