import{fireEvent,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it,vi}from'vitest';
vi.mock('./SkyModel',()=>({default:({mode,value,node}:{mode:string;value:number;node:number})=><div data-testid="sky-model">{mode}:{value}:{node}</div>}));
import App from'./App';
describe('eclipse laboratory',()=>{afterEach(()=>{location.hash=''});it('opens with the correct solar order, shows a miss, and switches to lunar order',()=>{location.hash='#eclipses';render(<App/>);expect(screen.getByText(/Sun → Moon → Earth · Moon’s umbra reaches Earth/)).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Moon above or below the Sun-Earth line'),{target:{value:'.3'}});expect(screen.getByText(/the shadow misses its target/)).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/Sun → Earth → Moon lunar/i}));expect(screen.getByText(/Sun → Earth → Moon · Moon enters Earth’s umbra/)).toBeInTheDocument()});it('keeps the guided journey reader-paced',()=>{render(<App/>);fireEvent.click(screen.getByRole('button',{name:/set the sky in motion/i}));expect(screen.getByText('Night is a place in shadow.')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/continue/i}));expect(screen.getByText('Tilt, carried around a star.')).toBeInTheDocument()})})
