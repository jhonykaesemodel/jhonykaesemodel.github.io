import{useEffect,useState}from'react';import SkyModel from'./SkyModel';import{eclipseType,modes,phaseName,seasonName,story,tideKind,type Mode}from'./model'
function Brand({lab=false}:{lab?:boolean}){return <span className="brand"><i/>CLOCKWORK SKY {lab&&<em>LAB</em>}<b>06</b></span>}
function Readout({mode,value,node}:{mode:Mode;value:number;node:number}){const a=value*Math.PI*2;let title='';let detail='';if(mode==='day'){title=`${String(Math.floor(value*24)).padStart(2,'0')}:00 local rotation`;detail='Drag through one turning Earth'}if(mode==='seasons'){title=seasonName(a);detail='Northern hemisphere label'}if(mode==='phases'){title=phaseName(a);detail=`Day ${Math.round(value*29.53)} of 29.5`}if(mode==='eclipses'){title=eclipseType(a,node);detail=Math.abs(node)<.13?'near an orbital node':'the Moon passes above or below'}if(mode==='tides'){title=tideKind(a);detail=tideKind(a)==='spring tide'?'Sun and Moon effects reinforce':'Sun and Moon effects partly oppose'}return <div className="readout"><span>MODEL REPORT</span><h3>{title}</h3><p>{detail}</p></div>}
function Info({close}:{close:()=>void}){return <div className="scrim"><section className="info"><button className="close" onClick={close} aria-label="Close">×</button><p className="eyebrow">MODEL & LIMITS</p><h2>Correct relationships. Impossible scale.</h2><p>Orbital order, axial tilt, illumination, and phase relationships are grounded in astronomy. Sizes, distances, speeds, shadows, colors, and tidal deformation are greatly amplified so everything can be seen together.</p><dl><div><dt>Orbits</dt><dd>Shown circular and smooth. Real orbits are elliptical and dynamically perturbed.</dd></div><div><dt>Light</dt><dd>The renderer approximates illumination; it does not calculate atmospheric scattering or precise eclipse paths.</dd></div><div><dt>Tides</dt><dd>The equilibrium bulge is explanatory. It cannot predict a tide at a coast, where basin shape and local conditions matter.</dd></div><div><dt>Controls</dt><dd>One slider compresses hours, months, or a year depending on the selected question.</dd></div></dl><h3>Scientific starting points</h3><a href="https://science.nasa.gov/earth/facts/" target="_blank">NASA: Earth rotation, orbit, and tilt ↗</a><a href="https://science.nasa.gov/moon/eclipses/" target="_blank">NASA: eclipses and lunar inclination ↗</a><a href="https://oceanservice.noaa.gov/education/tutorial_tides/tides03_gravity.html" target="_blank">NOAA: gravity and two tidal bulges ↗</a><a href="https://oceanservice.noaa.gov/facts/springtide.html/perigean-spring-tide.html" target="_blank">NOAA: spring and neap tides ↗</a><button className="primary" onClick={close}>Return to the sky</button></section></div>}
function Lab(){const[mode,setMode]=useState<Mode>('day'),[value,setValue]=useState(.12),[node,setNode]=useState(.25);return <><SkyModel mode={mode} value={value} node={node}/><nav className="modes">{modes.map((m,i)=><button key={m[0]} className={mode===m[0]?'active':''} onClick={()=>{setMode(m[0]);setValue(.12)}}><span>0{i+1}</span>{m[1]}</button>)}</nav><section className="controls"><p className="eyebrow">ONE SYSTEM · FIVE RHYTHMS</p><h2>{modes.find(m=>m[0]===mode)?.[2]}</h2><label>{mode==='day'?'Earth rotation':mode==='seasons'?'Year':mode==='phases'?'Lunar month':mode==='eclipses'?'Moon alignment':'Sun–Moon angle'}<input aria-label="Time position" type="range" min="0" max="1" step=".002" value={value} onChange={e=>setValue(+e.target.value)}/></label>{mode==='eclipses'&&<label>Orbital height<input aria-label="Orbital height" type="range" min="-.4" max=".4" step=".01" value={node} onChange={e=>setNode(+e.target.value)}/></label>}</section><Readout mode={mode} value={value} node={node}/></>}
export default function App(){const[state,setState]=useState<'landing'|'guided'|'lab'>(()=>location.hash==='#lab'?'lab':'landing'),[step,setStep]=useState(0),[info,setInfo]=useState(false);useEffect(()=>{const f=(e:KeyboardEvent)=>{if(state==='guided'&&e.key==='ArrowRight'){step===4?setState('lab'):setStep(v=>v+1)}if(state==='guided'&&e.key==='ArrowLeft')setStep(v=>Math.max(0,v-1));if(e.key==='Escape')setInfo(false)};addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[state,step]);if(state==='landing')return <main className="landing"><header><Brand/></header><div className="orbits" aria-hidden="true"><i/><i/><i/><b/></div><section className="hero"><p className="eyebrow">EARTH · MOON · SUN / ONE MOVING GEOMETRY</p><h1>The sky is<br/>a clock with <em>many hands.</em></h1><p>Turn one model and see why light becomes days, seasons, phases, eclipses, and tides.</p><button className="primary" onClick={()=>setState('guided')}>Set the sky in motion <span>→</span></button><button className="secondary" onClick={()=>setState('lab')}>Open orbital laboratory</button><small>Five movements · at your pace · drag to orbit</small></section><footer>Distances, sizes, and time are compressed. Relationships are preserved.</footer></main>;const item=story[step];return <main className={`experience ${state}`}><header className="top"><Brand lab={state==='lab'}/><button onClick={()=>state==='guided'?setState('lab'):setInfo(true)}>{state==='guided'?'Skip to lab →':'Model & limits ?'}</button></header>{state==='guided'?<><SkyModel mode={item.mode} value={step*.17+.12} node={step===3?.05:.25}/><section className="story" key={step}><span>{String(step+1).padStart(2,'0')} / 05</span><h2>{item.title}</h2><p>{item.body}</p><small>{item.note}</small><button onClick={()=>step===4?setState('lab'):setStep(v=>v+1)}>{step===4?'Enter the observatory':'Continue'} →</button></section><nav className="story-nav"><button aria-label="Previous" disabled={!step} onClick={()=>setStep(v=>Math.max(0,v-1))}>←</button><i>{step+1}/5</i><button aria-label="Next" onClick={()=>step===4?setState('lab'):setStep(v=>v+1)}>→</button></nav><div className="progress" style={{'--p':`${(step+1)/5*100}%`} as React.CSSProperties}/></>:<Lab/>}{info&&<Info close={()=>setInfo(false)}/>}<div className="grain"/></main>}
