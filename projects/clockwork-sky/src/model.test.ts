import{describe,expect,it}from'vitest';import{eclipseType,phaseName,seasonName,tideKind}from'./model';describe('orbital relationships',()=>{it('maps the lunar cycle',()=>{expect(phaseName(0)).toBe('new moon');expect(phaseName(Math.PI)).toBe('full moon')});it('maps northern seasons',()=>expect(seasonName(Math.PI/2)).toBe('June solstice'));it('requires alignment and a node for eclipse',()=>{expect(eclipseType(0,0)).toContain('solar');expect(eclipseType(0,.3)).toBe('no eclipse')});it('distinguishes spring and neap tides',()=>{expect(tideKind(0)).toBe('spring tide');expect(tideKind(Math.PI/2)).toBe('neap tide')})})
