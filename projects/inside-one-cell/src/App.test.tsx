import {fireEvent,render,screen} from '@testing-library/react'; import {describe,expect,it} from 'vitest'; import App from './App'; describe('experience',()=>{it('enters the reader-paced journey',()=>{render(<App/>);fireEvent.click(screen.getByText(/Enter the cell/i));expect(screen.getByText('Life keeps a border.')).toBeInTheDocument()});it('opens the lab directly',()=>{render(<App/>);fireEvent.click(screen.getByText(/Open expression lab/i));expect(screen.getByText('EXPRESS ONE GENE')).toBeInTheDocument()})})
