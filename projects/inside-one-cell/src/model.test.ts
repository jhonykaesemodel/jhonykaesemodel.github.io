import {describe,expect,it} from 'vitest'; import {codons,nextPhase,transcribe,translate} from './model'; describe('gene expression model',()=>{it('transcribes complementary RNA',()=>expect(transcribe('TACG')).toBe('AUGC'));it('groups codons and translates the modeled sequence',()=>{expect(codons('AUGCCU')).toEqual(['AUG','CCU']);expect(translate('AUGCCUUAA')).toEqual(['Met','Pro','Stop'])});it('holds at the final phase',()=>expect(nextPhase('fold')).toBe('fold'))})
