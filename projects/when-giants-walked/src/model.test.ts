import{describe,expect,it}from'vitest';import{aliveAt,creatures,gapBetween,overlapped,periodAt}from'./model';describe('deep-time model',()=>{it('maps period boundaries',()=>{expect(periodAt(220)).toBe('Triassic');expect(periodAt(180)).toBe('Jurassic');expect(periodAt(100)).toBe('Cretaceous')});it('filters creatures at an age',()=>expect(aliveAt(67).map(x=>x.name)).toContain('Tyrannosaurus'));it('finds contemporaries',()=>expect(overlapped(creatures[8],creatures[9])).toBe(true));it('separates Stegosaurus and Tyrannosaurus',()=>expect(gapBetween(creatures[2],creatures[8])).toBe(82))})
