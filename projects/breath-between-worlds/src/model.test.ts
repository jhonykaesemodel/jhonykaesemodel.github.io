import{describe,expect,it}from'vitest';import{destinations,move}from'./model';describe('carbon reservoir graph',()=>{it('allows photosynthesis from air to plant',()=>expect(move('air','leaf')).toBe('leaf'));it('contains fast and slow branches',()=>{expect(destinations('leaf')).toContain('body');expect(destinations('deep')).toContain('rock')});it('rejects invented direct routes',()=>expect(()=>move('air','rock')).toThrow())})
