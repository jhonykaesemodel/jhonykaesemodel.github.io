import{fireEvent,render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import App from'./App';describe('experience flow',()=>{it('moves from landing to guided story and lab',()=>{render(<App/>);fireEvent.click(screen.getByRole('button',{name:/follow the atom/i}));expect(screen.getByText('Carbon has no home.')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/skip to lab/i}));expect(screen.getByText('FOLLOW ONE CARBON ATOM')).toBeInTheDocument()})})
