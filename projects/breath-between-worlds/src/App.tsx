import{useEffect,useState}from'react';import{reservoirs,routes,story,type Reservoir}from'./model';
function Brand(){return <span className="brand"><i/>THE BREATH BETWEEN WORLDS <b>011</b></span>}
const edges:[Reservoir,Reservoir][]=[['air','leaf'],['air','ocean'],['leaf','body'],['leaf','soil'],['body','soil'],['soil','rock'],['ocean','deep'],['deep','rock'],['rock','air']];
function Cycle({here='air',trail=[]}:{here?:Reservoir;trail?:Reservoir[]}){return <svg className="instrument cycle" viewBox="0 0 100 105" role="img" aria-label={`Carbon atom in ${reservoirs[here].label}`}><defs><filter id="glow"><feGaussianBlur stdDeviation="1.2" result="b"/><feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs>{edges.map(([a,b],i)=>{const A=reservoirs[a],B=reservoirs[b];return <line key={i} x1={A.x} y1={A.y} x2={B.x} y2={B.y} className="route"/>})}{(Object.keys(reservoirs)as Reservoir[]).map(k=>{const r=reservoirs[k],active=k===here,visited=trail.includes(k);return <g key={k} transform={`translate(${r.x} ${r.y})`} className={`reservoir ${active?'active':''} ${visited?'visited':''}`}><circle r={active?4:2.2}/><text y="6">{r.label}</text><text y="9" className="time">{r.time}</text></g>})}<circle cx={reservoirs[here].x} cy={reservoirs[here].y} r="1" className="atom" filter="url(#glow)"/></svg>}
function Info({close}:{close:()=>void}){return <div className="scrim"><aside className="info"><button className="close" aria-label="Close" onClick={close}>×</button><p className="eyebrow">MODEL & LIMITS</p><h2>One atom through an immense system.</h2><p>Routes and timescale bands are grounded in the carbon cycle, but this atom's itinerary is chosen by you. The diagram is not a flux-balanced Earth-system model.</p><dl><div><dt>True here</dt><dd>Carbon moves among atmosphere, life, soils, oceans, sediment, and rock through distinct processes.</dd></div><div><dt>Amplified</dt><dd>A marked atom, simultaneous reservoirs, distances, and movement speed.</dd></div><div><dt>Omitted</dt><dd>Reservoir quantities, isotopes, spatial variation, feedbacks, and probabilistic flux rates.</dd></div></dl><h3>Scientific starting points</h3><a href="https://earthobservatory.nasa.gov/features/CarbonCycle" target="_blank">NASA Earth Observatory — the carbon cycle ↗</a><a href="https://earthobservatory.nasa.gov/features/OceanCarbon/page1.php" target="_blank">NASA — the ocean's carbon balance ↗</a><button className="primary" onClick={close}>Return to the atom</button></aside></div>}
function Lab(){const[here,setHere]=useState<Reservoir>('air'),[trail,setTrail]=useState<Reservoir[]>(['air']);const go=(r:Reservoir)=>{setHere(r);setTrail(t=>[...t,r].slice(-9))};return <><Cycle here={here} trail={trail}/><section className="panel"><p className="eyebrow">FOLLOW ONE CARBON ATOM</p><h2>Now: {reservoirs[here].label}.</h2><p className="measure">Typical stay <b>{reservoirs[here].time}</b></p><div className="big-actions route-actions">{routes[here].map((r,i)=><button key={`${r.to}${i}`} onClick={()=>go(r.to)}><small>{r.verb}</small>{reservoirs[r.to].label} →</button>)}</div><p className="hint">There is no single carbon-cycle itinerary. Choose one possible next transfer.</p><button className="quiet" onClick={()=>{setHere('air');setTrail(['air'])}}>Begin again in the air</button></section></>}
export default function App(){const[state,setState]=useState<'landing'|'guided'|'lab'>('landing'),[step,setStep]=useState(0),[info,setInfo]=useState(false);const places:Reservoir[]=['air','leaf','body','ocean','rock','air'];useEffect(()=>{const k=(e:KeyboardEvent)=>{if(state==='guided'&&e.key==='ArrowRight')step===5?setState('lab'):setStep(v=>v+1);if(state==='guided'&&e.key==='ArrowLeft')setStep(v=>Math.max(0,v-1))};addEventListener('keydown',k);return()=>removeEventListener('keydown',k)},[state,step]);if(state==='landing')return <main className="landing"><header><Brand/></header><div className="hero-art"><Cycle here="leaf"/></div><section className="hero"><p className="eyebrow">THE JOURNEY OF ONE ATOM</p><h1>Air becomes leaf.<br/>Leaf becomes <em>you.</em></h1><p>Follow one carbon atom through living bodies, open water, deep time—and back into breath.</p><button className="primary" onClick={()=>setState('guided')}>Follow the atom →</button><button className="quiet" onClick={()=>setState('lab')}>Open carbon laboratory</button></section></main>;return <main className="experience"><header className="top"><Brand/><button onClick={()=>state==='guided'?setState('lab'):setInfo(true)}>{state==='guided'?'Skip to lab →':'Model & limits ?'}</button></header>{state==='guided'?<><Cycle here={places[step]} trail={places.slice(0,step+1)}/><section className="story"><span>0{step+1} / 06</span><h2>{story[step].title}</h2><p>{story[step].body}</p><small>{story[step].note}</small><button onClick={()=>step===5?setState('lab'):setStep(v=>v+1)}>Continue →</button></section><nav className="pager"><button disabled={!step} onClick={()=>setStep(v=>v-1)}>←</button><i>{step+1}/6</i><button onClick={()=>step===5?setState('lab'):setStep(v=>v+1)}>→</button></nav></>:<Lab/>}{info&&<Info close={()=>setInfo(false)}/>}</main>}
